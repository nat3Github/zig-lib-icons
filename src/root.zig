pub const feather = @import("feather.zig");
pub const lucide = @import("lucide.zig");
pub const heroicons = struct {
    pub const outline = @import("heroicons-outline.zig");
    pub const solid = @import("heroicons-solid.zig");
};
